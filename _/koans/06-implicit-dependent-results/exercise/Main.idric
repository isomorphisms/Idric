module Main

import Data.Vect

-- The zero-capable cardinality is inferred from the compatibility Vect value.
prepend : {n : Cardinality} → a → Vect n a → Vect (S n) a
prepend item items = ?dependent_result

main : IO ()
main = printLn (prepend 1 [2, 3])
