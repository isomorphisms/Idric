module Main

import Data.Vect

%default total

-- n is inferred from values; the result type depends on that inferred value.
prepend : {n : ℕ} → a → Vect n a → Vect (S n) a
prepend item items = ?dependent_result

main : IO ()
main = printLn (prepend 1 [2, 3])
