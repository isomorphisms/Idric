module Main

%default total

increment : Nat → Nat
increment = \number ⇒ ?incremented

twice : (a → a) → a → a
twice = \function ⇒ \value ⇒ ?twice_result

main : IO ()
main = printLn (twice increment 5)
